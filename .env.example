# ── Frontend (Vite) ───────────────────────────────────────────────────────────
# URL of the deployed Render backend (no trailing slash)
VITE_API_URL=https://cv-yorumlayici-backend.onrender.com

# ── Backend (set these as Render environment variables, NOT here) ─────────────
GOOGLE_API_KEY=your_google_gemini_api_key_here
